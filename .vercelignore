# Local-only artifacts — not needed in the deployment (prod uses Turso, not the
# local SQLite file). src/server.js is intentionally NOT ignored: Vercel runs it
# as the Node server entrypoint.
data/
node_modules/
.env
